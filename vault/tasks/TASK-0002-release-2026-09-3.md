# TASK-0002 - 发布 2026.09.3 GitHub Release

<!-- trellium-task-state
{
  "schema_version": 1,
  "task_id": "TASK-0002",
  "level": "C",
  "authority_level": 3,
  "lifecycle": "accepted"
}
-->

## Objective

为既有 `2026.09.3` tag 创建 GitHub Release，使 `install.sh` 的 latest release 解析能够获取本轮状态层与 checker 修复。

## Scope

### In Scope

- 校正自托管试点 K4 为 measurement-only，与预注册实验一致。
- 核验 `97d5506`、`b8ee8ea`、tag、测试、Skill 快照和 `trellium check`。
- 创建并验证 GitHub Release `2026.09.3`。
- 更新本任务、runtime、handoff 与 shadow-run 事实记录。

### Out of Scope

- 改写 `2026.09.2` 或 `2026.09.3` tag。
- 修改 checker、协议模板、安装器或发布机制。
- 补建其他历史 Release。

## Context Required

- `AGENTS.md`
- `vault/index.md`
- `vault/runtime.md`
- `vault/governance.md`
- `vault/tasks/TASK-0001-self-hosting-pilot.md`
- `vault/details/shadow-run-2026-09.md`

## Capability Tags

- release
- documentation
- testing

## Authority

Allowed:

- 修改本仓库自托管 vault 记录与 K4 项目策略。
- 使用既有 tag 创建同名 GitHub Release。
- 提交并推送本任务产生的 vault 修改。

Requires Approval:

- 创建 GitHub Release 和修改项目预算策略；用户已在当前会话明确批准。

Forbidden:

- 移动、覆盖或删除既有 tag。
- 修改产品代码或协议内容。
- 伪造测试、发布或 shadow 观测结果。

## Acceptance Criteria

- [x] `vault/index.md` 不配置预算阈值，K4 只测量。
- [x] 87 项测试通过，Skill 快照同步，`check . --format json` 为 0 error / 0 warning。
- [x] GitHub Release `2026.09.3` 指向既有 tag `97d5506`，非 draft、非 prerelease。
- [x] GitHub latest release 能解析为 `2026.09.3`。
- [x] 自托管任务与 shadow-run 记录真实更新。

Optional（非 Gate，owner 2026-09-08 决定，见 D-0003）：

- [ ] Release 标题与正文 notes 补齐（当前 API 返回 `name=""`、`body=""`）。

## Verification

Required:

- `python3 -m unittest scripts.test_trellium scripts.test_sync_skills scripts.test_install_sh`
- `python3 scripts/sync-skills.py --check`
- `python3 scripts/trellium.py check . --format json`
- `gh release view 2026.09.3 --repo zlin101/trellium`

Completed:

- 87/87 tests passed.
- Skill protocol snapshots are in sync.
- Pre-release `check . --format json`: 0 errors / 0 warnings after TASK-0002 was committed.

## Execution Record

### 2026-09-04 - Agent: Codex

Context read:

- 自托管必读文件、TASK-0001、shadow-run 台账与发布增量。

Changes made:

- 创建本任务并将 K4 policy 校正为 measurement-only。

Checks run:

- `python3 -m unittest scripts.test_trellium scripts.test_sync_skills scripts.test_install_sh` → 87/87 passed.
- `python3 scripts/sync-skills.py --check` → both snapshots in sync.
- `python3 scripts/trellium.py check . --format json` → 0 errors / 0 warnings after commit `1d9d19b`.
- `gh release create ...` → 未执行外部请求，环境中没有 `gh` 可执行文件。

Review and reflection:

- 该发布是第二个真实任务；不把发布操作包装成新的发布框架。

Risks:

- GitHub Release 尚未创建；tags API 可见不等于 `releases/latest` 可解析。

Next action:

- 用户通过 GitHub Releases 网页为既有 `2026.09.3` tag 创建 Release；随后将本任务 blocked → active → accepted，并验证 latest release。

## Memory Updates

- `vault/runtime.md`
- `vault/details/shadow-run-2026-09.md`
- `vault/handoff.md`

### 2026-09-04 - Agent: Claude (GLM) — 阻塞缩小，未解除

Context read:

- GitHub Releases API 与 `releases/latest` 重定向的实际状态；`/tmp/trellium-2026-09-3-release-notes.md`。

Changes made:

- 无代码变更；仅更新本任务、shadow-run 台账与 handoff 事实记录。

Checks run:

- `GET /repos/zlin101/trellium/releases` → 已发布：2026.09.2（用户新建）、2026.09.0；**2026.09.3 Release 仍不存在**。
- `releases/latest` 重定向 → `…/tag/2026.09.2`（此前为 2026.09.0，用户创建 2026.09.2 Release 后改善）。
- `check . --format json` → 0 error / 0 warning。

Review and reflection:

- 用户的 Release 建在了 2026.09.2 tag 上（上一会话给的 notes 是 2026.09.2 版本说明）。这修复了 `install.sh` 只能装到 2026.09.0 的存量缺口，但本任务的验收（latest 解析到 2026.09.3）未达成。
- 准备好的 2026.09.3 notes 文件（1562 字节）完好，直接可用。

Risks:

- 若用户不再建 2026.09.3 Release，`install.sh` 将停在 2026.09.2，checker 修复对该安装路径不可见。

Next action:

- 用户二选一：(a) 打开 `https://github.com/zlin101/trellium/releases/new?tag=2026.09.3`，标题 `Trellium 2026.09.3 — Agent-native Vault checks`，正文粘贴 `/tmp/trellium-2026-09-3-release-notes.md`；或 (b) 提供带 `repo` 权限的 token 由 Agent 经 API 创建。完成后本任务 blocked → active → accepted。

### 2026-09-08 - Agent: GLM (ZCode) — 下一周期计划 M2 状态复核，阻塞未变

Context read:

- 下一周期计划（2026-09-08）第 7 节；GitHub Releases API 实时状态。

Changes made:

- 无代码变更；仅追加本复核记录。

Checks run:

- `GET /repos/zlin101/trellium/releases/latest` → HTTP 200，tag `2026.09.2`（draft=false，prerelease=false）。
- `GET /repos/zlin101/trellium/releases/tags/2026.09.3` → HTTP 404，Release 仍不存在。
- 本地：tag `2026.09.3` 仍指向 `97d5506`；`init/VERSION` = `2026.09.3`。

Review and reflection:

- 执行环境仍无 `gh`、无 GitHub 写凭据；按计划保持真实 blocked，不代建、不改判。

Next action:

- 与上一条相同：用户经 GitHub UI 为 `2026.09.3` tag 创建 Release（notes 备于 `/tmp/trellium-2026-09-3-release-notes.md`，若已清理可从 commit `97d5506` 的变更说明重建），或提供 token 由 Agent 经 API 创建；随后 blocked → active → accepted。

### 2026-09-08 - Agent: Codex — Release 已发布，元数据待补齐

Context read:

- TASK-0002 当前契约、2026-09-08 计划第 7 节、runtime、handoff 与 shadow ledger。

Changes made:

- 用户确认已发布后实时核验 GitHub；外部写能力 blocker 已解除，任务 blocked → active。
- 同步 TASK 状态块、runtime 投影、handoff 与 shadow-run 观测；未自动写 accepted。

Checks run:

- `GET /repos/zlin101/trellium/releases/latest` → HTTP 200，tag `2026.09.3`。
- `GET /repos/zlin101/trellium/releases/tags/2026.09.3` → HTTP 200，draft=false、prerelease=false、published_at=`2026-09-08T03:25:21Z`。
- `GET /repos/zlin101/trellium/git/ref/tags/2026.09.3` → commit `97d5506e52e2704536bfcc6fb77d0e52bebb9e13`，与既有 tag 一致。
- Release API 当前返回 `name=""`、`body=""`；计划第 7 节要求标题和 notes 与版本一致，技术验收尚未全部通过。
- `python3 scripts/trellium.py check . --format json` → 0 error / 0 warning。
- `python3 scripts/sync-skills.py --check` → 两套 snapshot in sync。
- `python3 -m unittest scripts.test_trellium scripts.test_sync_skills scripts.test_install_sh` → 87/87 OK。
- `git diff --check` → 通过。

Review and reflection:

- 发布路径已经解锁，`install.sh --fetch` 的 latest 解析不再停在 2026.09.2。
- 发布对象存在不等于元数据验收完成；在标题和正文补齐前保持 active。

Next action:

- 用户编辑现有 Release：标题设为 `Trellium 2026.09.3 — Agent-native Vault checks`，补入与 2026.09.3 一致的非空 notes；随后复核 API，完成技术验收并进入 ready_for_review。

### 2026-09-08 - Agent: GLM (ZCode) — owner 决定元数据降为可选，任务 accepted

Context read:

- Owner 修订方案「09.3 Post-release Validation」M0（标题与 notes 降为可选改进，不再作为阻塞 Gate）；上一条 Codex 复核记录；GitHub API 实时状态。

Changes made:

- 依据 owner 决定将"标题与 notes"从验收 Gate 降级为 Optional（非 Gate）条目，决策记入 `vault/decisions.md` D-0003；未删除该事实，空标题/正文仍如实保留。
- 状态块 active → accepted（owner 授权的技术验收关闭：Release 存在、tag 正确、非 draft/prerelease、latest 解析正确，全部经两轮独立现场核验）。
- 同步 runtime 投影、shadow ledger（转换行与计数）、handoff（本条目并入任务文件）。

Checks run:

- `GET /releases/latest` → HTTP 200，tag `2026.09.3`；`GET /releases/tags/2026.09.3` → HTTP 200，draft=false、prerelease=false（本会话独立复核，与 Codex 记录一致）。
- `python3 scripts/trellium.py check . --format json` → 0 error / 0 warning（提交前复跑）。

Review and reflection:

- 本任务的验收缺口从来不是"Release 是否存在"，而是元数据；owner 判定机器路径（tag + latest 解析）才是价值所在，人类阅读体验为可选——这与 install.sh 的实际依赖一致。

Risks:

- 空 notes 对人类访客不友好；作为 Optional 项保留，随时可补。

Next action:

- 无（accepted）。若 owner 日后补齐标题/notes，勾选 Optional 项即可，无需重开任务。
