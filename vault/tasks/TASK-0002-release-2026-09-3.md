# TASK-0002 - 发布 2026.09.3 GitHub Release

<!-- trellium-task-state
{
  "schema_version": 1,
  "task_id": "TASK-0002",
  "level": "C",
  "authority_level": 3,
  "lifecycle": "blocked"
}
-->

## Objective

为既有 `2026.09.3` tag 创建 GitHub Release，使 `install.sh` 的 latest release 解析能够获取本轮状态层与 checker 修复。

## Scope

### In Scope

- 校正自托管试点 K4 为 measurement-only，与预注册实验一致。
- 核验 `97d5506`、`b8ee8ea`、tag、测试、Skill 快照和 `trellium check`。
- 创建并验证 GitHub Release `2026.09.3`。
- 更新本任务、runtime、handoff 与 shadow-run 事实记录。

### Out of Scope

- 改写 `2026.09.2` 或 `2026.09.3` tag。
- 修改 checker、协议模板、安装器或发布机制。
- 补建其他历史 Release。

## Context Required

- `AGENTS.md`
- `vault/index.md`
- `vault/runtime.md`
- `vault/governance.md`
- `vault/tasks/TASK-0001-self-hosting-pilot.md`
- `vault/details/shadow-run-2026-09.md`

## Capability Tags

- release
- documentation
- testing

## Authority

Allowed:

- 修改本仓库自托管 vault 记录与 K4 项目策略。
- 使用既有 tag 创建同名 GitHub Release。
- 提交并推送本任务产生的 vault 修改。

Requires Approval:

- 创建 GitHub Release 和修改项目预算策略；用户已在当前会话明确批准。

Forbidden:

- 移动、覆盖或删除既有 tag。
- 修改产品代码或协议内容。
- 伪造测试、发布或 shadow 观测结果。

## Acceptance Criteria

- [x] `vault/index.md` 不配置预算阈值，K4 只测量。
- [x] 87 项测试通过，Skill 快照同步，`check . --format json` 为 0 error / 0 warning。
- [ ] GitHub Release `2026.09.3` 指向既有 tag `97d5506`，非 draft、非 prerelease。
- [ ] GitHub latest release 能解析为 `2026.09.3`。
- [ ] 自托管任务与 shadow-run 记录真实更新。

## Verification

Required:

- `python3 -m unittest scripts.test_trellium scripts.test_sync_skills scripts.test_install_sh`
- `python3 scripts/sync-skills.py --check`
- `python3 scripts/trellium.py check . --format json`
- `gh release view 2026.09.3 --repo zlin101/trellium`

Completed:

- 87/87 tests passed.
- Skill protocol snapshots are in sync.
- Pre-release `check . --format json`: 0 errors / 0 warnings after TASK-0002 was committed.

## Execution Record

### 2026-09-04 - Agent: Codex

Context read:

- 自托管必读文件、TASK-0001、shadow-run 台账与发布增量。

Changes made:

- 创建本任务并将 K4 policy 校正为 measurement-only。

Checks run:

- `python3 -m unittest scripts.test_trellium scripts.test_sync_skills scripts.test_install_sh` → 87/87 passed.
- `python3 scripts/sync-skills.py --check` → both snapshots in sync.
- `python3 scripts/trellium.py check . --format json` → 0 errors / 0 warnings after commit `1d9d19b`.
- `gh release create ...` → 未执行外部请求，环境中没有 `gh` 可执行文件。

Review and reflection:

- 该发布是第二个真实任务；不把发布操作包装成新的发布框架。

Risks:

- GitHub Release 尚未创建；tags API 可见不等于 `releases/latest` 可解析。

Next action:

- 用户通过 GitHub Releases 网页为既有 `2026.09.3` tag 创建 Release；随后将本任务 blocked → active → accepted，并验证 latest release。

## Memory Updates

- `vault/runtime.md`
- `vault/details/shadow-run-2026-09.md`
- `vault/handoff.md`
