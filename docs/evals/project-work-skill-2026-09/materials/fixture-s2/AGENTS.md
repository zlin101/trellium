# AGENTS

任何非琐碎任务前，按顺序读取：

1. `vault/index.md`（含任务与授权速查表）
2. `vault/runtime.md`

Level B/C、判定模糊或涉及治理规则时，追加读取 `vault/governance.md`。
追踪任务读取 `vault/tasks/` 下任务文件；中断恢复先读 `vault/handoff.md`。

Forbidden：不保存密钥；不伪造验证；不静默覆盖用户改动；runtime 行只是投影，不授权。
